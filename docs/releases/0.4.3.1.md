# v0.4.3.1

- 修复绝妖星 P1 部分绘图事件早于实体同步、阶段初始化尚未完成时，分摊或分散判断被跳过的问题。对相关事件增加有界顺序等待，保留原触发条件和配置。
- 补充绝伊甸、绝妖星绘图诊断：记录事件接收与转换、节点开关、原条件结果、关键变量、实体缺失或晚到、回调注册和异常，方便区分触发前置与绘图接口问题。
- 诊断仅在对应地图收集，并分别限制事件、节点和错误日志数量；不额外计算条件、不主动扫描游戏内存，也不修改订阅触发器。

更新后请完整重启游戏。使用仿生石时，退出游戏前先停止模拟并关闭防火墙。

绝伊甸缺圈尚未根据实战日志确定根因。本次增强诊断，不能视为所有缺圈问题已经修复。若仍出现缺圈，请保留该次 `dalamud.log`，并附发生时间、阶段和具体缺失效果。
